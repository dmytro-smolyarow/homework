// <util>
// pure utility — no Fastify dependency, no env access, no side effects
// callers pass everything they need as arguments

export const <util> = (input: <I>): <O> => {
  // …
  return /* … */
}
